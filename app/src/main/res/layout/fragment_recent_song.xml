<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="RecentSongFragment"
    android:fitsSystemWindows="true">

    <!-- TODO: Update blank fragment layout -->
    <ImageView
        android:id="@+id/img_song_background"
        android:layout_width="match_parent"
        android:layout_height="330dp"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/_f51c1b6061d427ab66915160903b32c"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"/>

    <TextView
        android:id="@+id/tv_recently_played"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@+id/img_song_background"
        app:layout_constraintEnd_toEndOf="@+id/img_song_background"
        app:layout_constraintTop_toTopOf="@id/img_song_background"
        android:text="@string/recently_played_song"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/white"/>

    <TextView
        android:id="@+id/tv_artist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/img_song_background"
        app:layout_constraintBottom_toBottomOf="@id/img_song_background"
        android:text="Uzaktan"
        android:textSize="20sp"
        android:textColor="@color/white"
        android:textStyle="italic"
        android:layout_marginStart="30dp"
        android:layout_marginBottom="30dp"/>

    <TextView
        android:id="@+id/tv_song_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/tv_artist"
        app:layout_constraintBottom_toTopOf="@id/tv_artist"
        android:text="When you know you know"
        android:textSize="40sp"
        android:textColor="@color/white"
        android:textStyle="italic" />

</androidx.constraintlayout.widget.ConstraintLayout>